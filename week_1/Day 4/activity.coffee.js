//Create a function that takes two parameters for a coffee order (size, type of drink)
const coffeOrder = (size, drinkType) => {
    console.log(`Here is your ${size} ${drinkType}`);
    }
    
    coffeOrder("large", "coffee");